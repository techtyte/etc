///Random things I never remember go here///

{{UPGRADE A SHELL}}

python3 -c "import pty;pty.spawn('/bin/bash')"
Ctrl-Z
stty raw -echo
fg
export TERM=xterm-256color

{{SUDOERS}}

cd /etc/sudoers.d/

{{MYSQL commands}}

mysql -u user -p -h [ip]: remote
mysql -u user -p : local

show databases;
use databasename;

show tables;
select * from tablename;

describe tablename;



Filter wordlist for specific lengths example:

sudo grep -x '.\{4\}' rockyou.txt > 4rockyou.txt


Download all files in index of pages:
wget -r -np -nH --cut-dirs=3 -R index.html http://website.com/path/to/files
